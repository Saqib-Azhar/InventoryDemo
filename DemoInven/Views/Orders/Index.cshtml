@model IEnumerable<DemoInven.Models.Order>

@{
    ViewBag.Title = "Index";
}

<h2>Orders</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table" style="border:double">
    <tr>
        <th>
            @Html.DisplayName("Id")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BuyerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BoughtAt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalPrice)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.IsDeleted)
        </th>*@
        <th>
            @Html.DisplayName("Cashier")
        </th>
        @*<th></th>*@
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <a href="/Orders/Details/@item.Id">@Html.DisplayFor(modelItem => item.Id)</a>
        </td>
        <td>
            <a href="/Orders/Details/@item.Id">@Html.DisplayFor(modelItem => item.BuyerName)</a>
        </td>
        <td>
            <a href="/Orders/Details/@item.Id">@Html.DisplayFor(modelItem => item.BoughtAt)</a>
        </td>
        <td>
            <a href="/Orders/Details/@item.Id"> @Html.DisplayFor(modelItem => item.TotalPrice)</a>
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.IsDeleted)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.Email)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
